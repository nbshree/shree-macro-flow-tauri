# 交易行助手与模块独占机制设计方案

> 状态：已实施。交易行助手已作为独立工作区接入应用。

## Summary

- 新增顶层“交易行助手”模块，拥有独立配置、模板、坐标、热键、日志和运行状态。
- 顶部页签切换即模块激活：切换时立即停止其他模块的运行、测试、采集、延时事件和专属热键；隐藏到托盘不算切换。
- 用户首次开始前打开搜索框；运行后高频点击搜索记录中的商品，识别购买图标后购买、重新打开搜索框并进入下一轮。
- 可设置购买点击次数；达到次数或商城状态图标连续 3 帧缺失后停止并清理运行资源。

## Key Changes

### 模块独占协调

- 扩展现有后端权威 `Workspace` 协调器，包含宏、游戏录制、Buff、交易行及两个计算器。
- 现有 `switch_workspace` 负责模块独占切换：
  - 同页签重复点击不停止当前任务。
  - 切换页签时停止所有旧模块活动并使旧线程、重连和延时回调失效。
  - 清除宏坐标录制、按键编辑和交易坐标采集状态。
  - 游戏回放输入释放失败时拒绝切换，页面保持原模块，全局紧急停止仍可用。
  - 成功后才更新前端页面、托盘菜单和模块热键。
- 全局只常驻 `Ctrl+Alt+Esc` 紧急停止；其余仅注册当前模块热键。
- 宏、游戏和交易模块允许使用相同热键，只校验模块内部重复及紧急停止冲突。
- 所有启动、配置和热键回调在 Rust 端再次校验当前激活模块，防止旧回调越权执行。

### 交易行配置

- 独立存储到应用数据目录下的 `trade-assistant/`，不与 Buff 配置或模板混用。
- 配置包含：
  - 目标游戏窗口、搜索区域、商品模板和遮罩。
  - 搜索记录商品、购买、打开搜索框三个绝对物理屏幕坐标。
  - 采集、开始、停止三个独立热键，默认分别为 `Ctrl+Alt+Q/P/O`。
  - 购买次数、20–1000ms 点位一点击间隔、购买确认帧数、两段循环延时和 0–10 秒启动倒计时。
  - 三个坐标与宏流程一致，保存为虚拟桌面绝对物理坐标，不绑定目标窗口边界。
- 坐标采集流程为：选择槽位 → 将鼠标移动到目标位置 → 按采集热键；采集坐标不要求提前配置或聚焦目标窗口。

### 图像识别与购买状态机

- 复用 Buff 助手的窗口枚举、截图、模板遮罩、灰度匹配和稳定帧检测能力；Buff 与交易模块保留各自独立状态机和存储。
- 前端复用并通用化现有图片配置向导：
  1. 选择游戏窗口并捕获预览。
  2. 分别框选购买图标和商城状态图标的搜索区域。
  3. 分别裁剪两个图标模板。
  4. 可选涂抹动态数字、价格或闪光区域。
  5. 实时查看置信度并测试模板。
- 两个模板固定使用 95% 阈值；购买图标默认连续 2 帧确认且可配置为 1–5 帧；商城图标连续 3 帧确认、连续 3 帧缺失停止。
- 单一串行运行线程负责全部时序，避免刷新与购买竞态：
  1.  启动双图标监听并进入配置的倒计时，倒计时期间不执行点击。
  2.  倒计时结束后验证商城状态图标；购买图标已出现则直接购买，否则开始高频点击点位一。
  3.  确认购买图标后原子停止点位一，立即点击点位二并按成功注入累计一次购买尝试。
  4.  达到购买次数后进入 `completed`；否则等待购买到搜索延时，点击点位三，再等待搜索到连点延时并开始下一轮。
  5.  新一轮必须先观察购买图标消失，避免上一轮残留图标触发重复购买。
- 窗口关闭、商城图标连续 3 帧缺失、点击注入失败或模板处理失败时立即停止并进入错误状态，不自动恢复。游戏窗口失去前台、移动或缩放不单独阻止运行，点击始终使用已采集的绝对屏幕坐标。
- 手动停止、切换模块或紧急停止发生在任何延时期间时，后续点击必须被取消。

### 前端与公开接口

- 新增 `tradeAssistant` 工作区和“交易行助手”页面，包含运行控制、配置完整性、坐标采集、热键与倒计时、图像向导、识别测试及日志。
- 六个顶部页签在窄窗口下改为两行布局，避免挤压品牌、更新和主题按钮。
- 交易模块激活时，托盘提供“开始抢购/停止抢购”。
- 新增 Rust/TypeScript 类型：
  - `TradeAssistantConfig`
  - `TradeCoordinates` / `TradeCoordinateSlot`
  - `TradeAssistantHotkeys`
  - `TradeAssistantActivity`
  - `TradeAssistantState`
- 新增命令覆盖状态读取、模板配置/测试、坐标采集、设置保存、开始和停止；新增 state、metric、execution-log 事件。
- 安装更新阻断逻辑加入交易倒计时、监听、暂停、购买和模板测试状态。

## Test Plan

- 验证切换每个模块都会停止旧模块任务、注销旧热键，并拒绝已排队的旧回调。
- 验证紧急停止覆盖宏录制/执行、游戏录制/回放、Buff 监控/测试和交易全部阶段。
- 验证倒计时、默认 50ms 连点、购买确认帧数、两段循环延时、购买计数及达到次数后不再点击点位三。
- 验证识别购买图标后不再点击点位一；停止或切页期间不会执行任何待定点击。
- 验证前台失焦不会停止；窗口关闭和商城状态图标缺失后立即停止；窗口移动后仍按已采集的绝对坐标点击。
- 验证负坐标、100%/125%/150% 缩放和混合 DPI 多显示器。
- 验证完成和错误状态均清理运行资源，但购买完成后保留模板、坐标和设置。
- 补充交易页面/controller、六页签导航、安装阻断及共享图像编辑组件回归测试。
- 依次运行前端测试与构建、Rust `fmt/check/clippy`，最后验证 Tauri Windows 构建，避免并行运行重型检查。

## Assumptions

- 第一版以点位二鼠标输入成功注入作为一次购买尝试，不识别“已售出、余额不足、购买失败”等结果，也不自动补偿。
- 坐标采用用户选择的绝对屏幕坐标；窗口移动或缩放后仍使用原坐标，用户可按需要手动重新采集。
- 不主动抢占或切换游戏焦点，也不因游戏失去前台而暂停或停止。
- 本次不更新版本号、不打标签、不执行正式发布。

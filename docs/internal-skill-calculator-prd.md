# 内功计算器页面级 PRD（内部原型）

> 状态：内部评审稿，不面向公开发布。规则来源为
> `防守内功计算器.xlsx` 的核心业务区 `B2:I31`。Excel 只用于规则核对与回归测试，不作为
> 运行时依赖，不随应用读取、解析、打包或分发。

## 1. 产品背景

现有软件主要解决宏流程的执行效率，内功计算器用于补充配装决策能力。原 Excel 已能给出
词条分、特性分、总分和档位，但输入分散、联动关系不透明，也不适合直接嵌入桌面应用。

本原型将表格规则重构为原生页面，让用户完成三件事：快速录入、理解分数来源、判断距离
下一档还差多少。原型验证规则正确性和页面可用性，不代表最终商业发布方案。

## 2. 目标与非目标

### 2.1 目标

- 完整承载 11 个基础属性、15 个内功的携带与“灵”状态。
- 实时输出词条分、特性分、总分、当前档位和下一档差值。
- 展示逐项贡献及灼星贯日的联动来源，使结果可解释、可复核。
- 使用版本化、确定性的应用内规则；同一输入始终得到同一结果。
- 以原表默认数据作为黄金样本，建立后续实现和回归测试基线。

### 2.2 非目标

- 不在 MVP 中提供截图 OCR、自动识别、最优组合搜索或游戏内自动操作。
- 不把 Excel 界面、单元格公式引擎或 Office/COM 能力搬入应用。
- 不从用户磁盘读取原工作簿，不依赖网络表格或远程规则服务。
- 不在规则约束未确认前强制“最多携带 N 个”或推断“灵”与携带的关系。
- 不宣称计算结果为官方标准，也不据此提供交易、付费或收益承诺。

## 3. 核心用户任务

1. 用户录入当前装备的基础属性。
2. 用户在同一内功行勾选“携带”和“灵”。
3. 用户立即看到总分、档位、分项贡献和距离下一档的差值。
4. 用户通过贡献明细和联动说明定位最有影响的属性或内功。
5. 用户默认从空白方案开始，可随时清空当前录入后重新计算。

## 4. 信息架构

将计算器作为独立一级工作区，不塞入现有宏流程侧栏或弹窗。

```text
应用
├─ 宏流程
└─ 内功评估
   └─ 当前计算（MVP）
      ├─ 基础属性
      ├─ 内功配置
      └─ 结果与贡献明细
```

P1 再在“内功评估”下增加已保存方案和 A/B 对比。主题仅影响视觉，不得改变计算规则；
职业主题也不得自动替换规则集。

## 5. 页面结构

### 5.1 页头

- 标题：`内功评估`。
- 状态标识：`内部原型`。
- 只读规则标识：`7.20 新世界 · 防守团`，同时显示内部规则版本
  `new-world-defense-7.20`。
- 操作：`清空全部`。无输入时按钮禁用；存在录入时清空当前草稿。
- 来源入口：打开页面内的规则来源与免责声明，不跳转不明外链。

### 5.2 主体布局

默认窗口宽度下采用三栏：基础属性、内功配置、结果。结果栏在页面滚动时保持可见。

```text
┌ 基础属性 ──────┬ 内功配置 ─────────────────────┬ 结果 ────────────┐
│ 11 个数值输入  │ 内功名称  携带  灵  本项贡献   │ 总分 / 当前档位  │
│ 输入单位提示   │ 15 行 + 联动说明              │ 距下一档差值     │
│                │                               │ 贡献明细         │
└────────────────┴───────────────────────────────┴──────────────────┘
```

- 窗口空间不足时结果栏移至下方，不产生全局横向滚动。
- 内功区使用表格语义；“携带”和“灵”位于同一行，贡献同时展示词条与特性来源。
- 结果区优先级依次为总分、档位、下一档差值、两类分数、贡献明细。
- 分数主展示保留 2 位小数；展开明细保留 4 位小数。档位判断使用未四舍五入的原始总分。

## 6. 字段清单与黄金测试样本

页面首次进入时保持空白。下列黄金样本仅用于自动化测试和规则回归，不作为页面默认值，也不
提供“载入示例”入口。

### 6.1 基础属性（11 项）

所有字段均为大于等于 0 的十进制数。标注“百分号前”的字段直接填写百分号前数值，例如
`4.7%` 填 `4.7`，不能填 `0.047`。

| 字段 ID            | 展示名称               | 计分系数/公式 | 黄金值 |
| ------------------ | ---------------------- | ------------: | -----: |
| `season`           | 赛年（填百分号前）     |    `x × 0.86` |      1 |
| `strengthOrQi`     | 力量/气海              |  `x × 0.1415` |    100 |
| `attack`           | 攻击                   |   `x × 0.043` |    100 |
| `armorPenetration` | 破防                   |   `x × 0.034` |    100 |
| `factionRestraint` | 流派克制（填百分号前） |    `x × 0.94` |      1 |
| `criticalHit`      | 会心                   |  `x × 0.0149` |    100 |
| `maxAttack`        | 最大攻击               |  `x × 0.0215` |    100 |
| `minAttack`        | 最小攻击               |  `x × 0.0215` |    100 |
| `agility`          | 身法                   |  `x × 0.0894` |    100 |
| `endurance`        | 耐力                   |   `x × 0.043` |    100 |
| `constitution`     | 根骨                   |   `x × 0.043` |    100 |

### 6.2 内功（15 项）

每项包含两个独立布尔字段：`equipped`（是否携带）与 `spirit`（是否为灵）。MVP 不因一个字段
改变另一个字段，也不自动取消用户选择。“灵”的布尔假设仍需来源方确认。

| 内功 ID             | 展示名称       | 携带时特性分 |   灵的词条分 | 黄金携带 | 黄金灵 |
| ------------------- | -------------- | -----------: | -----------: | :------: | :----: |
| `zhuoXingGuanRi`    | 灼星贯日       |         6.25 | 动态，见 7.2 |    是    |   是   |
| `chengYingFengShuo` | 承影锋烁       |            6 |            3 |    是    |   是   |
| `jueDianJingSha`    | 绝电惊沙       |          5.5 |         2.46 |    是    |   否   |
| `riYueLiangYi`      | 日月两仪       |          4.9 |          2.2 |    否    |   否   |
| `chuKuangGe`        | 楚狂歌         |          5.1 |          1.7 |    是    |   否   |
| `zhongMiao`         | 众妙           |         5.25 |         1.75 |    是    |   是   |
| `fenRen`            | 焚刃           |          4.7 |          1.6 |    否    |   否   |
| `zhanJing`          | 斩精           |          4.5 |          1.5 |    否    |   否   |
| `poFu`              | 破釜           |            5 |          1.6 |    是    |   否   |
| `guanShanYue`       | 贯山月（卡轴） |          3.9 |          1.3 |    否    |   否   |
| `duanHanMang`       | 锻寒芒         |          4.8 |          1.6 |    否    |   否   |
| `jiShuai`           | 击衰           |          4.5 |          1.5 |    否    |   否   |
| `jingYu`            | 惊羽           |          4.5 |          1.5 |    否    |   否   |
| `caiFeng`           | 裁锋           |            5 |         1.65 |    否    |   否   |
| `wuYunYao`          | 五韵谣         |          4.5 |          1.3 |    否    |   否   |

应用继续使用既有展示名称“承影锋烁”和“贯山月（卡轴）”，以保持界面与 AI 识别协议兼容。
工作簿中的对应名称分别为“承影锋镝”和“贯山月”；该差异不改变内功 ID 或计分值。

### 6.3 黄金输出

上述默认输入必须得到以下未舍入结果：

| 输出     |     期望值 |
| -------- | ---------: |
| 词条分   |     55.405 |
| 特性分   |       33.1 |
| 总分     |     88.505 |
| 档位     |       泰斗 |
| 距下一档 | 已达最高档 |

## 7. 计算规则

### 7.1 总体公式

```text
词条分 = 11 项基础属性得分之和 + 15 项灵得分之和
特性分 = 所有已携带内功的特性分之和
总分   = 词条分 + 特性分
```

计算过程使用完整精度，不对中间项逐项四舍五入。显示层舍入不得反向参与总分或档位计算。
规则应固化为应用内的版本化结构数据和确定性计算函数；Excel 不参与运行时计算。

### 7.2 灼星贯日联动

定义联动基数：

```text
linkedBase = 5.5
  +（众妙为灵 ? 7 : 众妙已携带 ? 5.25 : 0）
  +（绝电惊沙为灵 ? 8 : 绝电惊沙已携带 ? 5.5 : 0）
```

- 灼星贯日已携带时，特性分固定为 `6.25`；未携带为 0。
- 灼星贯日为灵时，词条分为 `1.875 + 0.1 × linkedBase`；不为灵时为 0。
- 在众妙、绝电惊沙行以及灼星贯日行展示“影响灼星贯日”的联动标识。
- 用户变更任一关联项后，灼星贯日的灵贡献和总分同步更新；普通携带分保持 `6.25`。
- `灵` 优先于同一内功的普通携带值参与联动；即使同时勾选，也只采用灵分支一次。

### 7.3 档位边界

按工作簿区间文案采用“下限包含、上限不包含”：

| 原始总分区间    | 档位     |
| --------------- | -------- |
| `< 57`          | 建议转生 |
| `>= 57 且 < 63` | 豪杰     |
| `>= 63 且 < 70` | 宗师     |
| `>= 70`         | 泰斗     |

边界使用原始总分判断。例如 `69.9999` 仍属于宗师，显示成 `70.00` 也不能提前升档。

“距下一档”只在存在下一档时显示，计算为下一档下限减原始总分；最高档显示“已达最高档”。

## 8. 交互与状态

### 8.1 实时计算

- 有效输入变化后立即本地计算，无需提交按钮、加载动画或网络请求。
- 数值框支持键盘录入、全选和清除；空数值按 0 参与计算。
- `清空全部`将数值、携带和灵清零；无草稿内容时按钮禁用。
- 改动后标记页面为“未保存”仅留作 P1；MVP 不暗示数据已持久化。
- 展开贡献明细时按“基础属性、灵、携带特性”分组，默认隐藏 0 贡献项，可切换显示全部。

### 8.2 验证与错误

- 数值仅接受有限、非负十进制；不设置未经确认的业务上限，也不静默截断或修正。
- 负数、无穷值或无法解析的内容显示字段级错误；存在错误时总结果显示“请修正输入”，不输出新档位。
- 百分比字段始终显示 `%` 后缀和“填写百分号前数值”提示。
- 携带数仅显示 `已携带 N`，在最大数量未确认前不阻止继续选择。
- 携带与灵允许独立组合；MVP 不把看似异常的组合视为错误。

### 8.3 空状态

首次进入或清空后，若所有数值为 0 且没有任何携带或灵选择，结果区显示：
`填写属性或选择内功后查看评估结果`。此时不展示“建议转生”，避免把尚未开始录入误解为真实
评估。

### 8.4 可访问性

- 每个输入都有持久可见的文本标签，不以 placeholder 代替；错误提示与字段通过
  `aria-describedby` 关联。
- 内功使用有表头的表格语义；复选框可通过 Tab 到达，名称需包含内功名和动作，例如
  “携带灼星贯日”“灼星贯日为灵”。
- 所有操作支持键盘，不依赖悬停。
- 焦点环清晰，文字对比度至少 4.5:1，状态不能只用颜色表达。
- 总分摘要更新使用克制的 `aria-live="polite"`，连续输入时避免逐键重复朗读完整明细。
- 尊重 `prefers-reduced-motion`；主题装饰和纯净模式不得影响字段可读性或点击区域。

## 9. 版本范围

### MVP：内部原型

- 独立“内功评估”页面及本 PRD 全部字段。
- 实时计算、档位、下一档差值、携带计数、分组贡献明细。
- 灼星贯日联动解释。
- 空白首屏、清空、输入验证、空状态和来源说明。
- 版本化规则与黄金样本、边界、联动的自动化测试。
- 不持久化方案，不导入/导出，不采集原始输入。

### P1：可日常使用

- Rust 侧持久化多个计算方案，统一通过受限 Tauri API 调用，不使用 `localStorage` 作为正式来源。
- A/B 方案对比、差异高亮、最近使用和 JSON 导入/导出。
- 生成本地结果图片；图片明确带规则版本与“非官方”标识。
- 正式版新建方案的默认值策略、携带数量规则和来源授权落定。

### P2：扩展能力

- 多赛季、多流派规则包与旧版本只读复算。
- 截图 OCR 与人工校对流程。
- 在携带数量及游戏约束确认后提供最优组合建议。
- 经签名、校验和回滚保护的规则更新机制；仍不以 Excel 作为运行时格式。

## 10. 埋点建议

内部原型默认不启用遥测。若后续产品已有用户同意的匿名遥测能力，只记录聚合行为，不记录
基础属性数值、内功组合、方案名称、截图或设备路径。

| 事件                           | 触发时机                 | 建议属性                         |
| ------------------------------ | ------------------------ | -------------------------------- |
| `calculator_viewed`            | 首次进入页面             | `ruleVersion`、`appVersion`      |
| `calculator_first_result`      | 本次访问首次产生有效结果 | `ruleVersion`、`tier`            |
| `calculator_cleared`           | 清空全部                 | `hadValidResult`                 |
| `calculator_validation_failed` | 出现输入错误             | `fieldId`、`errorType`，不含原值 |
| `calculator_detail_toggled`    | 展开或收起贡献明细       | `expanded`                       |

不要对每次键入上报事件；同一错误应在一次编辑会话内去重。

## 11. 验收标准

### 11.1 规则正确性

- 黄金样本得到 `55.405 / 33.1 / 88.505`，误差不超过 `0.000001`，档位显示“泰斗”。
- 11 个基础系数、15 个携带分和 15 个灵分均有参数化回归覆盖。
- 黄金样本中把绝电惊沙改为灵后，灼星贯日普通携带贡献不变、灼星贯日灵贡献增加
  `0.25`、绝电惊沙灵贡献增加 `2.46`，总分合计增加 `2.71`。
- 分别覆盖 `56.9999、57、63、70`，档位符合第 7.3 节边界定义。
- 显示舍入不改变档位；所有 0 贡献项不会产生负零或 `NaN`。

### 11.2 页面与交互

- 11 个基础字段和 15 行携带与灵均可仅用键盘完成操作。
- 空白首屏、清空、错误状态和空状态与第 8 节一致。
- 每个非零贡献可追溯到具体字段或内功，灼星贯日联动值可解释。
- 1080×700、1280×820、最大化，以及 100%、125%、150% DPI 下无全局横向滚动或控件遮挡。
- 所有内置主题和纯净模式下均可读；主题切换不改变任何分数。

### 11.3 技术边界

- 安装包不读取或打包该 Excel，不引入 Office/COM、通用文件系统或 Shell 权限。
- 规则有稳定版本 ID，计算层可在无 UI、无 Tauri、无网络环境下执行自动化测试。
- 方案持久化未进入 MVP；P1 实现时磁盘读写必须留在 Rust，并通过受限 API 暴露。
- 实现提交至少通过前端测试、`pnpm typecheck`、`pnpm build` 及项目规定的 Rust 检查。

## 12. 授权、来源与发布风险

工作簿内声明：由“月望舒（逆水寒手游）”制作、无偿分享；感谢“杰少、满天星河、智齿”
提供帮助；计算公式来源于“折字愿为安”。“无偿分享”不能自动解释为允许商业集成、修改、
再分发或去除署名。

公开发布前必须完成：

- 取得制作方和公式来源方对应用集成、商业场景、修改与持续维护范围的明确授权。
- 确认最终署名文案、展示位置、是否允许内置规则及是否需要附原始链接。
- 不直接打包工作簿及其中图片；素材授权与公式授权分别核对。
- 明示规则版本、适用赛季/流派、非官方属性及可能随游戏更新失效的风险。
- 无法取得足够授权时，本功能保持内部验证，不进入公开安装包。

## 13. 待确认问题

| 问题                                  | 内部原型临时处理                      | 确认后影响                                       |
| ------------------------------------- | ------------------------------------- | ------------------------------------------------ |
| “灵”是否严格只能为 0/1？              | 使用布尔复选框                        | 若允许数量或等级，需改控件、公式、数据结构与迁移 |
| 最多可携带多少个内功，是否固定为 6？  | 只计数，不限制、不警告                | 决定校验、优化器约束和方案合法性                 |
| 7.20 规则的适用赛季和失效条件是什么？ | 使用 `new-world-defense-7.20` 版本 ID | 决定页面说明和后续规则更新策略                   |
